Implement cli functionality

Add Active initialization via cli
	required argument -i uses Ticker to instanciate an "Active" class e.g. "AAPL"
		"Accept multiple at once??"
	it should initiate a main loop to refresh and keep track of prices 
	while Active: (Every decision should be logged into the decision log and saved to "disk")
		update prices
		calculate angle
		calculate trend
		calculate change from previous angle 
		send notification when criteria is met 
	

Add to main loop to check prices every 15m or other "T" based on yfinance options
	make it configurable via cli -f --frequency
		# yfinance doc : Valid intervals: 1m,2m,5m,15m,30m,60m,90m,1h,1d,5d,1wk,1mo,3mo Intraday data cannot extend last 60 days
